# Calculadora-de-Matriz---C
Uma calculadora de matriz escrita em C

## como executar? (Linux, WSL-Windows, Termux)

Instalação: `apt install clang`

Compilação: `gcc consM.c -o ./matriz.o`

Execução: `./matriz.o`
